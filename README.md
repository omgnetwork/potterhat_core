_Too many dependency clashes with `omisego/ewallet`, and so it is archived as it does not have an immediate use._

# Potterhat Core

Potterhat's core features that can be included as an Elixir project's dependency.
